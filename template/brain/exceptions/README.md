# exceptions

The "we don't do it that way here" cases. One per file. Name the rule it breaks and the reason it's allowed to.

```
# <exception>
Breaks: (which how-we-work process / decision)
When: (the condition)
Why it's allowed:
Who can invoke it:
```
